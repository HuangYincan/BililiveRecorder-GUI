{
  "name": "bililive-recorder-gui",
  "private": true,
  "version": "2.20.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/check-no-custom-ui.mjs",
    "tauri": "tauri",
    "sidecar:prepare": "node scripts/prepare-sidecar.mjs",
    "upstream:sync": "node scripts/sync-upstream.mjs",
    "desktop:bump": "node scripts/bump-desktop-version.mjs",
    "desktop:version-test": "node --test scripts/desktop-version.test.mjs",
    "release:manifest": "node scripts/generate-release-manifest.mjs",
    "release:validate": "node scripts/validate-release.mjs",
    "bundle:smoke": "node scripts/verify-packaged-sidecar.mjs",
    "desktop:dev": "npm run sidecar:prepare && tauri dev",
    "desktop:build": "npm run sidecar:prepare && tauri build",
    "check": "npm run build && cargo check --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "2.11.5",
    "adm-zip": "0.6.1"
  }
}
